<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:Robotron"
             x:Class="Robotron.MainPage">

    <StackLayout x:Name="mainStackLayout" Orientation="Vertical" HorizontalOptions="Center" VerticalOptions="CenterAndExpand">
        <StackLayout x:Name="topInMainStackLayout" Orientation="Horizontal">
            <Button Text = "Cтаршие ДЗ" FontSize="Large" BorderWidth="1"
                    HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand"
                    Clicked="OnElderHomeButton" />
            <Button Text = "Старшие экз." FontSize="Large" BorderWidth="1"
                    HorizontalOptions="EndAndExpand" VerticalOptions="CenterAndExpand"
                    Clicked="OnElderExamButton" />
        </StackLayout>
        <StackLayout x:Name="bottomInMainStackLayout" Orientation="Horizontal">
            <Button Text = "Младшие ДЗ" FontSize="Large" BorderWidth="1"
                    HorizontalOptions="StartAndExpand" VerticalOptions="CenterAndExpand"
                    Clicked="OnYoungerHomeButton" />
            <Button Text = "Младшие экз." FontSize="Large" BorderWidth="1"
                    HorizontalOptions="EndAndExpand" VerticalOptions="CenterAndExpand"
                    Clicked="OnYoungerExamButton" />
        </StackLayout>
    </StackLayout>

</ContentPage>
